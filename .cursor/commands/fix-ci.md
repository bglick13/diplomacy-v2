# Fix CI

## Lint + Type check
- Fix all lint and type erros in the codebase. If there isn't an easy and obvious fix, add an ignore with a TODO to remove the ignore
- Run local equivalent of commands in .github/workflows/ci.yml to confirm CI will pass